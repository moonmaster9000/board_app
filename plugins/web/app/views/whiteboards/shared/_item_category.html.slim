section.item-category
  h2
    = t("#{category}.title")

  aside
    = link_to fa_icon("plus 2x"), url_for(controller: category, action: :new, whiteboard_id: @whiteboard.id)

  hr

  = render partial: "whiteboards/shared/#{category}_item_list"
