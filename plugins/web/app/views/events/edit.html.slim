= render partial: "shared/errors", locals: { errors: @errors }

= render partial: "event_form", locals: { submit_text: "Update Event", event: @event, url: whiteboard_event_path(params[:whiteboard_id], params[:id]), http_method: :put }
