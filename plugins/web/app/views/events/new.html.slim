= render partial: "shared/errors", locals: { errors: @errors }

= render partial: "event_form", locals: { submit_text: "Create Event", event: @event, url: whiteboard_events_path(params[:whiteboard_id]), http_method: :post }
